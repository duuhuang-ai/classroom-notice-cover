# 独立本地工具规则

## 边界

- 用户已于本次发布明确授权公开 GitHub 仓库并启用 GitHub Pages；在线页面只托管静态代码和背景，文案合成与使用记录仍在访问者浏览器内处理。

- GitHub 仓库范围仅限本工具目录；包含页面代码与 83 张运行所需背景，排除 `output/`、`.playwright-cli/`、本机配置与秘密信息。不包含上级项目素材和文案。

- 本目录是独立的本地网页工具，不属于“小红书商家工具箱”。
- 不读取、不修改、不复制商家工具箱的代码或构建产物。
- 不需要登录、密钥、后端、数据库或网络请求。
- 所有文案、图片合成和打包均在用户本机浏览器完成。

## 目录结构

- `index.html`：网页入口，跳转到含 83 张背景的双栏页面。
- `legacy.html`：保留的早期单栏页面。
- `styles.css`：页面样式和响应式规则。
- `app.js`：文案解析、场景分配、Canvas 合成、预览和 ZIP 导出。
- `prototypes/scene-usage.mjs`：原型的筛选、分配和记录校验纯逻辑。
- `prototypes/cover-export.mjs`：原型的本地图片合成和 ZIP 打包。
- `logic.mjs`：可在浏览器和 Node 测试中共用的纯逻辑，包括文案解析、场景序列和文件名处理。
- `assets/scenes/`：内置 3:4 课堂空屏背景，只保存工具使用的副本。
- `data/scenes.js`：正式工具的场景名称、标签和屏幕四角坐标。
- `output/`：本地验证产物；`output/playwright/` 保存浏览器截图和素材坐标检查图，不作为正式素材使用。
- `scripts/`：场景素材的离线检查和坐标辅助脚本，不参与浏览器运行。
- `tests/`：无浏览器依赖的核心逻辑测试。
- `prototypes/`：用户确认前的静态或可点击交互原型，与正式工具代码隔离；文件名使用 `prototype-vNN.html`。
- `start.command`：macOS 双击启动入口。
- `README.md`：非技术使用说明。

## 功能约定

- 双栏原型只编辑屏幕文案，不设置或显示笔记标题；预览卡以页码标识。旧三列表格中的末列标题仅在解析时忽略，不保存为页面字段。

- 输入区支持智能识别、每行一条、整段一条三种拆分模式。
- 智能识别支持 Markdown 标题、数字编号、空行分段和单句输入；无法判断结构的连续非空行默认按每行一条处理。
- 生成前必须展示拆分预览；预览内容可编辑、删除、添加、按换行拆开或并入上一条，最终以预览区内容为准。
- 单独输入一句文字也必须识别为 1 条通知。
- 通知页只有纯白背景和黑色主文字，不显示分类小字、工具名或水印。
- 每批场景不连续重复；数量超过背景数时重新洗牌。
- 场景文件统一使用 `scene-NN.png` 命名；新增场景从现有最大编号继续递增，不覆盖旧图。
- 每个场景在本地记录成功下载次数；预览、换场景和重新排版不计数，单张 PNG 下载成功或 ZIP 打包成功后才计数。
- 自动分配优先选择使用次数最少的场景；相同次数随机，并避免相邻结果重复。
- 使用记录支持 JSON 导出和导入，场景卡显示“未使用”或“已用 N 次”。
- 导出图片统一为 1080×1440 PNG。
- 超过4行或过长文字允许继续生成，但必须显示明确警告。
- 支持单张编辑、换场景、下载，以及全部 ZIP 下载。

## 视觉约定

- 工具界面使用“教师备课台”语义：纸白、黑板黑、木色和少量批改红。
- 主标题使用宋体系中文，正文使用苹方/系统黑体，数量和文件名使用等宽字体。
- 不使用装饰性渐变、大面积玻璃拟态或无含义统计卡片。
- 标志性交互是可视化的“场景胶卷”：生成结果沿场景顺序分配，换场景时像切换幻灯片。

## 验证

- 核心解析和场景分配逻辑必须有自动化测试，包括单句、Markdown 标题、数字编号、空行分段和三种拆分模式。
- 必须在真实浏览器中验证：1条、10条、20条、超过20条、单张换场景、单张下载、ZIP 下载和移动端无横向溢出。
- 图片质量必须抽查文字可读性、屏幕透视和 1080×1440 尺寸。
- 不得把本地预览或构建通过表述为线上部署成功。

## 近距离场景导入

- `data/all-scenes.mjs` 为双栏页面 v02/v03 共用的完整场景清单，包含全部 83 张背景及归一化屏幕四角；不再在页面内维护部分清单。
- `scripts/build-scene-catalog.py` 用于补齐场景定位，保留已有坐标；检查图保存在 `output/playwright/`，核对后才接入页面。

- `data/closeup-scenes-v01.json` 保存23 张近景工具副本的来源与归一化屏幕四角，供原型接入和追溯。
- 原型 `prototype-v02.html` 优先分配新增场景，保留已有场景；斜拍文字区域按屏幕四角透视映射。

## 场景使用筛选

- 筛选为全部、0 次、1 次、2 次、3 次、4 次及以上；筛选影响新分配和换场景，不偷偷使用筛选外场景。
- 没有匹配背景时显示空状态并禁止生成下载；只有一个匹配背景时提示允许重复。
- 使用次数以生成导出文件并交给浏览器下载为准，网页无法检测用户在保存窗口中取消；失败不计数。
- 记录以场景 ID 存储在本浏览器 localStorage；导入按各场景较大次数合并，避免重复导入叠加。
- 下载后保留已生成图片和场景，更新计数；下次换场景或分配时按当前筛选重新选择。

## 筛选菜单页面预览

- `prototypes/prototype-v03.html` 为独立可点击预览；v02 保留。
- v03 筛选为全部、未使用、恰好 1 次、2 次及以下（含 0）、3 次及以下（含 0）、3 次及以上。
- 筛选菜单放在重新分配课堂左侧；不显示大块场景列表，导入导出收进菜单底部。
- 预览默认展示三条示例及展开菜单，不自动下载、不改写使用记录。
